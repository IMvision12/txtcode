# Example environment variables for docker-compose

# GPU Configuration
# Specify which GPU(s) each engine should use
# Use comma-separated values for multiple GPUs
VLLM_GPU=0
SGLANG_GPU=0
TENSORRT_GPU=0

# Memory Configuration
VLLM_MEMORY=0.9
SGLANG_MEMORY=0.9
TENSORRT_MEMORY=0.9

# Model Configuration (optional - can be set in benchmark.json instead)
# VLLM_MODEL=meta-llama/Meta-Llama-3-8B-Instruct
# SGLANG_MODEL=meta-llama/Meta-Llama-3-8B-Instruct
# TENSORRT_MODEL=meta-llama/Meta-Llama-3-8B-Instruct
