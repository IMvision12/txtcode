FROM lmsysorg/sglang:latest

# Set environment variables
ENV PYTHONUNBUFFERED=1

# Install additional dependencies
RUN pip install fastapi uvicorn psutil requests nvidia-ml-py

# Create app directory
WORKDIR /app

# Copy server script
COPY servers/sglang_server.py /app/server.py

# Expose port
EXPOSE 8001

# Health check
HEALTHCHECK --interval=30s --timeout=10s --start-period=60s --retries=3 \
    CMD python3 -c "import requests; requests.get('http://localhost:8001/health')"

# Run server
CMD ["python3", "server.py", "--host", "0.0.0.0", "--port", "8001"]
